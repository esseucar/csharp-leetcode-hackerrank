public class Solution {
    public bool IsPalindrome(int x) {
        string num = x.ToString();
        int len= num.Length;
        bool result=true;
        for(int i=0; i<len/2;i++){
            if(num[i]==num[len-i-1])
            {
               result=true;
               continue; 
            }
            else
            {
                result=false;
                break; 
            }
        }return result;
    }
}

/* Alternatif yol

public class Solution {
    public bool IsPalindrome(int x) 
    {
        string num = x.ToString();
        bool isPalindrome = true;
        int len = num.Length;

        for (int i = 0; i < len / 2; i++)
        {
            // Directly compare the characters from the front and the back
            if (num[i] != num[len - 1 - i])
            {
                isPalindrome = false;
                break; // Exit early since it's confirmed not to be a palindrome
            }
        }return isPalindrome;
    }
} */
