/* public class Solution {
    public int RomanToInt(string s) {
        int sum = 0;
        int len = s.Length;

        for (int i = 0; i < len; i++) {
            int currentVal = GetValue(s[i]);

            // If this isn't the last character, look ahead to check for subtraction rules
            if (i + 1 < len && currentVal < GetValue(s[i + 1])) {
                sum -= currentVal;
            } else {
                sum += currentVal;
            }
        }

        return sum;
    }

    // Helper method to cleanly map characters to their integer values
    private int GetValue(char c) {
        switch (c) {
            case 'I': return 1;
            case 'V': return 5;
            case 'X': return 10;
            case 'L': return 50;
            case 'C': return 100;
            case 'D': return 500;
            case 'M': return 1000;
            default: return 0;
        }
    }
}
*/
